_target_: src.datasets.severity_dataset.SeverityDataset
mode: ${mode}
data_path: ${connected_dir}/data
dataset_name: ${dataset_name}
columns_mapping_file_path: ${connected_dir}/${columns_mapping_file_path}
timedelta_features: ${timedelta_features}
is_all_features: ${is_all_features}
unusing_features: ${unusing_features}
label_encoder_path: ${connected_dir}/${label_encoder_path}
label_column_name: ${label_column_name}
scale_type: ${scale_type}